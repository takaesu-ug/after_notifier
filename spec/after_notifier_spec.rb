require 'spec_helper'

describe AfterNotifier do
  it 'has a version number' do
    expect(AfterNotifier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
